import 'package:flutter/material.dart';

class DoctorHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Doctor Dashboard')),
      body: Center(
        child: Text('Doctor Home Screen - Coming Soon'),
      ),
    );
  }
}
